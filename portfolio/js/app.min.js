for(var showData=[],i=0;i<data.length;i++)data[i].show&&showData.push(data[i]);var vm=new Vue({el:"#app",data:{works:data,shows:showData},methods:{renderItem:function(a){return a.url?'<a href="'+a.url+'" target="_blank">'+a.name+"</a>":a.name}}});